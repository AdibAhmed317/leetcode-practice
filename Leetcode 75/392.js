const s = 'abc';
const t = 'ahbgdc';
const maxN = Math.max(s.length, t.length);
const sArr = [];
const tArr = [];
const sSubArr = [];
const tSubArr = [];

for (let i = 0; i < s.length; i++) {
  sArr.push(s.charAt(i));
}

for (let i = 0; i < t.length; i++) {
  tArr.push(t.charAt(i));
}

for (let i = 0; i < maxN; i++) {
  for (let j = 0; j < maxN; j++) {}
}
